# shutil Library in Python (Usage & Methods)
# The shutil module in Python is used for file and 
# directory management tasks such as copying, moving, deleting, and archiving.


# import shutil
# total, used, free = shutil.disk_usage(r"d:")
# print(f"Total: {total} bytes, Used: {used} bytes, Free: {free} bytes")
# import shutil

# python_path = shutil.which("python")
# print(f"Python is installed at: {python_path}")

